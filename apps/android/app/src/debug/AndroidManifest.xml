<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <receiver
            android:name=".DebugAudioTraceReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="ai.openclaw.app.DEBUG_AUDIO_TRACE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
