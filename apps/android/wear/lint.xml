<?xml version="1.0" encoding="utf-8"?>
<lint>
    <issue id="AndroidGradlePluginVersion" severity="ignore" />
    <issue id="GradleDependency" severity="ignore" />
    <issue id="IconLauncherShape" severity="ignore" />
    <issue id="NewerVersionAvailable" severity="ignore" />

    <!-- OpenClaw uses date-based version codes (yyyyMMddNN), which are high but still below the Android max. -->
    <issue id="HighAppVersionCode" severity="ignore" />

    <!-- Target SDK follows the current release train; bump only after platform compatibility testing. -->
    <issue id="OldTargetApi" severity="ignore" />

    <!-- WatchMainActivity extends ComponentActivity (activity 1.x), which fully supports ActivityResultContracts.
         This lint check fires incorrectly when no Fragment dependency is on the classpath. -->
    <issue id="InvalidFragmentVersionForActivityResult" severity="ignore" />

    <!-- Launcher icon assets are pending a design pass; the watch app is companion-paired (standalone=false). -->
    <issue id="MissingApplicationIcon" severity="ignore" />

    <!-- allowBackup=false and no user data; fullBackupContent for pre-API-31 is not required. -->
    <issue id="DataExtractionRules" severity="ignore" />
</lint>
