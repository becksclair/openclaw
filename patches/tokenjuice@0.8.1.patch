diff --git a/dist/cli/main.d.ts b/dist/cli/main.d.ts
index 893dba5faab63a7b2666007654bd270a296e35d9..cd90f159eb04525f6728d1ea9a8346eb7a591862 100644
--- a/dist/cli/main.d.ts
+++ b/dist/cli/main.d.ts
@@ -14,6 +14,7 @@ type ParsedArgs = {
     tee: boolean;
     raw: boolean;
     noOmit: boolean;
+    recordStats: boolean;
     storeDir: string | undefined;
     maxInlineChars: number | undefined;
     maxCaptureBytes: number | undefined;
diff --git a/dist/cli/main.js b/dist/cli/main.js
old mode 100644
new mode 100755
index 334dce65a31729146e3c7c5e3bcca267b1979492..941ed572ed786686291e4c0ee927af3b8e1aac7f
--- a/dist/cli/main.js
+++ b/dist/cli/main.js
@@ -50,7 +50,7 @@ import { doctorCodeRabbitConfig, installCodeRabbitConfig, uninstallCodeRabbitCon
 import { doctorCodeBuddyHook, installCodeBuddyHook, runCodeBuddyPreToolUseHook, uninstallCodeBuddyHook } from "../hosts/codebuddy/index.js";
 import { doctorCommandCodeHook, installCommandCodeHook, runCommandCodePostToolUseHook, uninstallCommandCodeHook } from "../hosts/command-code/index.js";
 import { doctorContinueRule, installContinueRule, uninstallContinueRule } from "../hosts/continue/index.js";
-import { doctorCodexHook, installCodexHook, runCodexPostToolUseHook, uninstallCodexHook } from "../hosts/codex/index.js";
+import { doctorCodexHook, installCodexHook, runCodexPostToolUseHook, runCodexPreToolUseHook, uninstallCodexHook, } from "../hosts/codex/index.js";
 import { doctorCopilotAgentHook, installCopilotAgentHook, runCopilotAgentPostToolUseHook, uninstallCopilotAgentHook } from "../hosts/copilot-agent/index.js";
 import { doctorCopilotCliHook, getCopilotCliInstructionsSnippet, installCopilotCliHook, runCopilotCliPostToolUseHook, uninstallCopilotCliHook, } from "../hosts/copilot-cli/index.js";
 import { doctorCrushSkill, installCrushSkill, uninstallCrushSkill } from "../hosts/crush/index.js";
@@ -357,6 +357,7 @@ export function parseArgs(argv) {
     let tee = false;
     let raw = false;
     let noOmit = false;
+    let recordStats = true;
     let storeDir;
     let maxInlineChars;
     let maxCaptureBytes;
@@ -437,6 +438,10 @@ export function parseArgs(argv) {
                 noOmit = true;
                 index += 1;
                 break;
+            case "--no-record-stats":
+                recordStats = false;
+                index += 1;
+                break;
             case "--tee":
                 tee = true;
                 index += 1;
@@ -519,6 +524,7 @@ export function parseArgs(argv) {
         tee,
         raw,
         noOmit,
+        recordStats,
         storeDir,
         maxInlineChars,
         maxCaptureBytes,
@@ -627,7 +633,7 @@ async function runWrap(args) {
         ...(args.raw ? { raw: true } : {}),
         ...(noOmit ? { noOmit: true } : {}),
         ...(args.trace ? { trace: true } : {}),
-        recordStats: true,
+        recordStats: args.recordStats,
         ...(args.store ? { store: true } : {}),
         ...(args.storeDir ? { storeDir: args.storeDir } : {}),
         ...(typeof args.maxInlineChars === "number" ? { maxInlineChars: args.maxInlineChars } : {}),
@@ -6547,6 +6553,8 @@ async function main(argv = process.argv.slice(2)) {
             return await runStats(args);
         case "codex-post-tool-use":
             return await runCodexPostToolUseHook(await readStdin(args.maxInputBytes));
+        case "codex-pre-tool-use":
+            return await runCodexPreToolUseHook(await readStdin(args.maxInputBytes), args.wrapLauncher);
         case "claude-code-pre-tool-use":
             return await runClaudeCodePreToolUseHook(await readStdin(args.maxInputBytes), args.wrapLauncher);
         case "claude-code-post-tool-use":
diff --git a/dist/core/integrations/compact-bash-result.d.ts b/dist/core/integrations/compact-bash-result.d.ts
index 75e537bb43afbbddddbb78e04219cf63773fa84a..c15883861b7ac42c1a394b9532c5769d4ced91af 100644
--- a/dist/core/integrations/compact-bash-result.d.ts
+++ b/dist/core/integrations/compact-bash-result.d.ts
@@ -12,6 +12,9 @@ export type CompactBashResultInput = {
     noOmit?: boolean;
     storeRaw?: boolean;
     metadata?: Record<string, unknown>;
+    boundedKeep?: {
+        maxInlineChars: number;
+    };
     inspectionPolicy?: InspectionCommandPolicy;
     /** @deprecated use inspectionPolicy instead. */
     skipInspectionCommands?: boolean;
@@ -28,6 +31,7 @@ export type CompactBashResultOutput = {
     rawText: string;
     usedTrustedFullText: boolean;
     result: CompactResult;
+    fallbackReason?: CompactBashResultKeepReason;
 };
 export declare function getOutputAwareInspectionSkipReason(policy: InspectionCommandPolicy, executionInput: ToolExecutionInput): InspectionCommandSkipReason | null;
 export declare function compactBashResult(input: CompactBashResultInput): Promise<CompactBashResultOutput>;
diff --git a/dist/core/integrations/compact-bash-result.js b/dist/core/integrations/compact-bash-result.js
index 9c9a7e144c02c2e0757863c1c4132c6ecea6b623..4ec646966a894a43360724741f88e3ff0dcbca01 100644
--- a/dist/core/integrations/compact-bash-result.js
+++ b/dist/core/integrations/compact-bash-result.js
@@ -1,8 +1,70 @@
 import { getInspectionCommandSkipReason, getSafeRepositoryInventorySourceArgv } from "../inventory-safety.js";
 import { readNoOmissionFromEnv } from "../env.js";
+import { storeArtifact, storeArtifactMetadata } from "../artifacts.js";
 import { buildInspectionSummary } from "../reduce-inspection-summary.js";
 import { reduceExecution } from "../reduce.js";
+import { mergeCompactionMetadata } from "../compaction-metadata.js";
+import { clipMiddleWithHashToBudget, MIN_HASHED_CLIP_BUDGET_CHARS } from "../reduce-utils.js";
+import { countTextChars } from "../text.js";
 import { getCompactionSkipReason } from "./rewrite-policy.js";
+function buildBoundedFallbackResult(rawText, result, clipped) {
+    const rawChars = result?.stats.rawChars ?? countTextChars(rawText);
+    const reducedChars = countTextChars(clipped.text);
+    return {
+        ...(result ?? {
+            classification: {
+                family: "preserved-output",
+                confidence: 0,
+            },
+        }),
+        inlineText: clipped.text,
+        ...(clipped.compaction
+            ? { compaction: mergeCompactionMetadata(result?.compaction, clipped.compaction) }
+            : {}),
+        stats: {
+            rawChars,
+            reducedChars,
+            ratio: rawChars === 0 ? 1 : reducedChars / rawChars,
+        },
+    };
+}
+async function applyBoundedKeepFallback(input, keep, executionInput) {
+    const maxInlineChars = input.boundedKeep?.maxInlineChars;
+    if (typeof maxInlineChars !== "number"
+        || !Number.isInteger(maxInlineChars)
+        || maxInlineChars < MIN_HASHED_CLIP_BUDGET_CHARS) {
+        return keep;
+    }
+    const clipped = clipMiddleWithHashToBudget(keep.rawText, maxInlineChars);
+    if (clipped.text === keep.rawText) {
+        return keep;
+    }
+    let result = buildBoundedFallbackResult(keep.rawText, keep.result, clipped);
+    if (!keep.result) {
+        const artifactInput = {
+            input: executionInput,
+            rawText: keep.rawText,
+            classification: result.classification,
+            stats: result.stats,
+        };
+        if (input.storeRaw) {
+            result = {
+                ...result,
+                rawRef: await storeArtifact(artifactInput),
+            };
+        }
+        else {
+            await storeArtifactMetadata(artifactInput);
+        }
+    }
+    return {
+        action: "rewrite",
+        rawText: keep.rawText,
+        usedTrustedFullText: keep.usedTrustedFullText,
+        result,
+        fallbackReason: keep.reason,
+    };
+}
 function resolveInspectionPolicy(input) {
     if (input.inspectionPolicy) {
         return input.inspectionPolicy;
@@ -51,12 +113,12 @@ export async function compactBashResult(input) {
     };
     const inspectionSkipReason = getOutputAwareInspectionSkipReason(resolveInspectionPolicy(input), executionInput);
     if (inspectionSkipReason) {
-        return {
+        return applyBoundedKeepFallback(input, {
             action: "keep",
             reason: inspectionSkipReason,
             rawText,
             usedTrustedFullText,
-        };
+        }, executionInput);
     }
     const options = {
         ...(typeof input.cwd === "string" && input.cwd.trim() ? { cwd: input.cwd } : {}),
@@ -68,13 +130,13 @@ export async function compactBashResult(input) {
     const result = await reduceExecution(executionInput, options);
     const skipReason = getCompactionSkipReason(command, rawText, result, input);
     if (skipReason) {
-        return {
+        return applyBoundedKeepFallback(input, {
             action: "keep",
             reason: skipReason,
             rawText,
             usedTrustedFullText,
             result,
-        };
+        }, executionInput);
     }
     return {
         action: "rewrite",
diff --git a/dist/core/reduce-utils.d.ts b/dist/core/reduce-utils.d.ts
index 750124fe17f941c97262a2a4341c5364238b0a62..784072c68c925c33e570a0bfecd68fe7db96f74f 100644
--- a/dist/core/reduce-utils.d.ts
+++ b/dist/core/reduce-utils.d.ts
@@ -1,5 +1,10 @@
 import { type CompactionMetadata } from "./compaction-metadata.js";
 export declare function compactWhitespace(text: string): string;
+export declare const MIN_HASHED_CLIP_BUDGET_CHARS = 64;
+export declare function clipMiddleWithHashToBudget(text: string, maxChars: number): {
+    text: string;
+    compaction?: CompactionMetadata;
+};
 export declare function clipMiddleWithHash(text: string, maxChars: number, noOmit?: boolean): {
     text: string;
     compaction?: CompactionMetadata;
diff --git a/dist/core/reduce-utils.js b/dist/core/reduce-utils.js
index 858fb1bb7987f028e00aff3a25630cbf448a5832..31fb5cc9b7efc853bad015a633e7ce49dbb91d76 100644
--- a/dist/core/reduce-utils.js
+++ b/dist/core/reduce-utils.js
@@ -7,8 +7,11 @@ export function compactWhitespace(text) {
 function shortHash(text) {
     return createHash("sha256").update(text).digest("hex").slice(0, 12);
 }
+export const MIN_HASHED_CLIP_BUDGET_CHARS = 64;
 function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
-    if (countTextChars(text) <= maxChars) {
+    const boundedMaxChars = Math.max(0, Math.floor(maxChars));
+    const inputChars = countTextChars(text);
+    if (inputChars <= boundedMaxChars) {
         return { text };
     }
     if (noOmit) {
@@ -17,16 +20,27 @@ function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
             compaction: createPassthroughCompactionMetadata("no-omit-char-clip-passthrough"),
         };
     }
-    const omitted = countTextChars(text) - maxChars;
-    const marker = `...[${omitted} chars omitted, sha256:${shortHash(text)}]...`;
-    const markerChars = countTextChars(marker);
-    if (maxChars <= markerChars) {
+    const buildMarker = (omitted) => `...[${omitted} chars omitted, sha256:${shortHash(text)}]...`;
+    let omitted = inputChars;
+    let marker = buildMarker(omitted);
+    let markerChars = countTextChars(marker);
+    if (boundedMaxChars <= markerChars) {
         return {
-            text: sliceTextChars(marker, 0, maxChars),
+            text: sliceTextChars(marker, 0, boundedMaxChars),
             compaction: createCompactionMetadata("hashed-middle-clip"),
         };
     }
-    const bodyChars = maxChars - markerChars;
+    let bodyChars = boundedMaxChars - markerChars;
+    for (;;) {
+        const nextOmitted = inputChars - bodyChars;
+        if (nextOmitted === omitted) {
+            break;
+        }
+        omitted = nextOmitted;
+        marker = buildMarker(omitted);
+        markerChars = countTextChars(marker);
+        bodyChars = Math.max(0, boundedMaxChars - markerChars);
+    }
     const headChars = Math.ceil(bodyChars * 0.7);
     const tailChars = Math.max(0, bodyChars - headChars);
     return {
@@ -34,6 +48,12 @@ function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
         compaction: createCompactionMetadata("hashed-middle-clip"),
     };
 }
+export function clipMiddleWithHashToBudget(text, maxChars) {
+    if (!Number.isInteger(maxChars) || maxChars < MIN_HASHED_CLIP_BUDGET_CHARS) {
+        return { text };
+    }
+    return clipMiddleWithHashStrict(text, maxChars);
+}
 function minifyJsonLexically(rawText) {
     const text = rawText.trim();
     let output = "";
diff --git a/dist/hosts/codex/index.d.ts b/dist/hosts/codex/index.d.ts
index 9d7c7ead7e9cb36d58f7fe24bcffd29c29355174..a1896f7b654e48f43be7e9b705c5674d9dc8fe7e 100644
--- a/dist/hosts/codex/index.d.ts
+++ b/dist/hosts/codex/index.d.ts
@@ -2,6 +2,7 @@ export type InstallCodexHookResult = {
     hooksPath: string;
     backupPath?: string;
     command: string;
+    preCommand: string;
     featureFlag: CodexFeatureFlagStatus;
 };
 export type CodexFeatureFlagStatus = {
@@ -54,7 +55,9 @@ export type CodexDoctorReport = {
     issues: string[];
     fixCommand: string;
     expectedCommand: string;
+    expectedPreCommand: string;
     detectedCommand?: string;
+    detectedPreCommand?: string;
     checkedPaths: string[];
     missingPaths: string[];
     featureFlag: CodexFeatureFlagStatus;
@@ -87,6 +90,7 @@ export declare function parseCodexFeatureFlag(source: string, keys: "hooks" | "c
     value: boolean | null;
 };
 export declare function inspectCodexRuntimeConfig(configPath?: string): Promise<CodexRuntimeConfigStatus>;
+export declare function runCodexPreToolUseHook(rawText: string, wrapLauncher?: string | undefined): Promise<number>;
 export declare function installCodexHook(hooksPath?: string, options?: CodexHookCommandOptions): Promise<InstallCodexHookResult>;
 export declare function uninstallCodexHook(hooksPath?: string): Promise<UninstallCodexHookResult>;
 export declare function doctorCodexHook(hooksPath?: string, options?: CodexHookCommandOptions): Promise<CodexDoctorReport>;
diff --git a/dist/hosts/codex/index.js b/dist/hosts/codex/index.js
index 203d6a0daf7f7c339309ced56f7b0cb0b614db99..889f9be4cee303149058200d12a2b0fa1b6ddde9 100644
--- a/dist/hosts/codex/index.js
+++ b/dist/hosts/codex/index.js
@@ -5,14 +5,17 @@ import { homedir } from "node:os";
 import packageJson from "../../../package.json" with { type: "json" };
 import { stripLeadingCdPrefix } from "../../core/command.js";
 import { storeArtifactMetadata } from "../../core/artifacts.js";
-import { compactBashResult, getOutputAwareInspectionSkipReason } from "../../core/integrations/compact-bash-result.js";
+import { compactBashResult } from "../../core/integrations/compact-bash-result.js";
 import { classifyOnly } from "../../core/reduce.js";
+import { MIN_HASHED_CLIP_BUDGET_CHARS } from "../../core/reduce-utils.js";
 import { countTextChars, stripAnsi } from "../../core/text.js";
 import { extractHookCommandPaths, isNodeExecutablePath, parseShellWords, shellQuote } from "../shared/hook-command.js";
 import { buildCompactionHint } from "../shared/hook-output.js";
+import { buildWrappedCommand, commandAlreadyWrapped, resolveHostShell, } from "../shared/pre-tool-wrap.js";
 const GENERIC_FALLBACK_MIN_SAVED_CHARS = 120;
 const GENERIC_FALLBACK_MAX_RATIO = 0.75;
 const HOOK_REWRITE_MIN_SAVED_CHARS = 8;
+const DEFAULT_CODEX_MAX_INLINE_CHARS = 1200;
 const CODEX_HOOK_LAST_LOG = "tokenjuice-hook.last.json";
 const CODEX_HOOK_HISTORY_LOG = "tokenjuice-hook.history.jsonl";
 const CODEX_HOOK_HISTORY_LIMIT = 200;
@@ -23,6 +26,7 @@ const LOW_NON_TOKENJUICE_TIMEOUT_SECONDS = 2;
 const RECOMMENDED_NON_TOKENJUICE_TIMEOUT_SECONDS = 6;
 const TOKENJUICE_CODEX_HOOK_TIMEOUT_SECONDS = 10;
 const TOKENJUICE_CODEX_STATUS = "compacting bash output with tokenjuice";
+const TOKENJUICE_CODEX_PRE_STATUS = "wrapping bash output with tokenjuice";
 const TOKENJUICE_CODEX_FIX_COMMAND = "tokenjuice install codex";
 function getCodexHome() {
     return process.env.CODEX_HOME || join(homedir(), ".codex");
@@ -226,23 +230,27 @@ async function resolveInstalledTokenjuicePath() {
     }
     return undefined;
 }
-async function buildCodexHookCommand(options = {}) {
+async function buildCodexHookCommand(options = {}, subcommand = "codex-post-tool-use") {
     const rawBinaryPath = options.binaryPath ?? process.argv[1];
     const binaryPath = rawBinaryPath && !isAbsolute(rawBinaryPath) ? resolve(rawBinaryPath) : rawBinaryPath;
     const nodePath = options.nodePath ?? process.execPath;
     if (!binaryPath) {
         throw new Error("unable to resolve tokenjuice binary path for codex install");
     }
+    let launcher = binaryPath;
     if (!options.local) {
         const installedBinaryPath = await resolveInstalledTokenjuicePath();
         if (installedBinaryPath) {
-            return `${shellQuote(installedBinaryPath)} codex-post-tool-use`;
+            launcher = installedBinaryPath;
         }
     }
-    if (binaryPath.endsWith(".js")) {
-        return `${shellQuote(nodePath)} ${shellQuote(binaryPath)} codex-post-tool-use`;
-    }
-    return `${shellQuote(binaryPath)} codex-post-tool-use`;
+    const launcherCommand = launcher.endsWith(".js")
+        ? `${shellQuote(nodePath)} ${shellQuote(launcher)}`
+        : shellQuote(launcher);
+    const wrapLauncherArg = subcommand === "codex-pre-tool-use"
+        ? ` --wrap-launcher ${shellQuote(launcher)}`
+        : "";
+    return `${launcherCommand} ${subcommand}${wrapLauncherArg}`;
 }
 function getCodexFixCommand(local = false) {
     return local ? "tokenjuice install codex --local" : TOKENJUICE_CODEX_FIX_COMMAND;
@@ -317,14 +325,14 @@ function stringifyToolResponse(value) {
     }
     return String(value);
 }
-function createTokenjuiceCodexHook(command) {
+function createTokenjuiceCodexHook(command, statusMessage) {
     return {
         matcher: "^Bash$",
         hooks: [
             {
                 type: "command",
                 command,
-                statusMessage: TOKENJUICE_CODEX_STATUS,
+                statusMessage,
                 timeout: TOKENJUICE_CODEX_HOOK_TIMEOUT_SECONDS,
             },
         ],
@@ -334,22 +342,30 @@ function isTokenjuiceCodexHook(group) {
     return group.hooks.some((hook) => {
         const command = typeof hook.command === "string" ? hook.command : "";
         return hook.statusMessage === TOKENJUICE_CODEX_STATUS
+            || hook.statusMessage === TOKENJUICE_CODEX_PRE_STATUS
+            || command.includes("codex-pre-tool-use")
             || command.includes("codex-post-tool-use")
             || command.includes("post_tool_use_tokenjuice.py");
     });
 }
 function findTokenjuiceCodexHookCommand(config) {
-    return findTokenjuiceCodexHook(config)?.command;
+    return findTokenjuiceCodexHook(config, "PostToolUse", "codex-post-tool-use")?.command;
+}
+function findTokenjuiceCodexPreHookCommand(config) {
+    return findTokenjuiceCodexHook(config, "PreToolUse", "codex-pre-tool-use")?.command;
 }
-function findTokenjuiceCodexHook(config) {
-    for (const group of config.hooks.PostToolUse ?? []) {
+function findTokenjuiceCodexHook(config, eventName, subcommand) {
+    for (const group of config.hooks[eventName] ?? []) {
         if (!isTokenjuiceCodexHook(group)) {
             continue;
         }
         const hook = group.hooks.find((hook) => {
             const hookCommand = typeof hook.command === "string" ? hook.command : "";
-            return hook.statusMessage === TOKENJUICE_CODEX_STATUS
-                || hookCommand.includes("codex-post-tool-use")
+            const statusMessage = eventName === "PreToolUse"
+                ? TOKENJUICE_CODEX_PRE_STATUS
+                : TOKENJUICE_CODEX_STATUS;
+            return hook.statusMessage === statusMessage
+                || hookCommand.includes(subcommand)
                 || hookCommand.includes("post_tool_use_tokenjuice.py");
         });
         if (hook) {
@@ -398,7 +414,7 @@ function collectLowTimeoutWarnings(config) {
     const issues = [];
     for (const [eventName, groups] of Object.entries(config.hooks)) {
         groups.forEach((group, groupIndex) => {
-            const tokenjuiceGroup = eventName === "PostToolUse" && isTokenjuiceCodexHook(group);
+            const tokenjuiceGroup = isTokenjuiceCodexHook(group);
             if (tokenjuiceGroup) {
                 return;
             }
@@ -420,14 +436,12 @@ function collectLowTimeoutWarnings(config) {
     return issues;
 }
 function collectTokenjuiceHookTimeoutWarnings(config, fixCommand) {
-    const hook = findTokenjuiceCodexHook(config);
-    if (!hook) {
-        return [];
-    }
-    if (hook.timeout !== TOKENJUICE_CODEX_HOOK_TIMEOUT_SECONDS) {
-        return [
-            `configured Codex tokenjuice hook timeout is missing or stale; run ${fixCommand} to add the ${TOKENJUICE_CODEX_HOOK_TIMEOUT_SECONDS}s safety cap`,
-        ];
+    const hooks = [
+        findTokenjuiceCodexHook(config, "PreToolUse", "codex-pre-tool-use"),
+        findTokenjuiceCodexHook(config, "PostToolUse", "codex-post-tool-use"),
+    ].filter((hook) => hook !== undefined);
+    if (hooks.some((hook) => hook.timeout !== TOKENJUICE_CODEX_HOOK_TIMEOUT_SECONDS)) {
+        return [`configured Codex tokenjuice hook timeout is missing or stale; run ${fixCommand} to add the ${TOKENJUICE_CODEX_HOOK_TIMEOUT_SECONDS}s safety cap`];
     }
     return [];
 }
@@ -539,11 +553,63 @@ function commandRequestsTokenjuiceRawBypass(command) {
     const optionArgs = argv.slice(wrapIndex + 1, optionEndIndex === -1 ? undefined : optionEndIndex);
     return optionArgs.includes("--raw") || optionArgs.includes("--full");
 }
-function buildCodexFeedback(inlineText, rawRefId) {
-    return `${inlineText}\n\n${buildCompactionHint(rawRefId)}`;
+export async function runCodexPreToolUseHook(rawText, wrapLauncher = process.argv[1]) {
+    let payload;
+    try {
+        payload = JSON.parse(rawText);
+    }
+    catch {
+        return 0;
+    }
+    const command = payload.tool_input?.command;
+    if (process.platform === "win32"
+        || payload.hook_event_name !== "PreToolUse"
+        || payload.tool_name !== "Bash"
+        || typeof command !== "string"
+        || !command.trim()
+        || !wrapLauncher
+        || commandAlreadyWrapped(command)
+        || commandRequestsTokenjuiceRawBypass(command)) {
+        return 0;
+    }
+    const shellPath = await resolveHostShell([
+        process.env.TOKENJUICE_CODEX_SHELL,
+        process.env.SHELL,
+        "bash",
+        "sh",
+    ]);
+    if (!shellPath) {
+        return 0;
+    }
+    const maxInlineChars = readIntegerEnvAtLeast("TOKENJUICE_CODEX_MAX_INLINE_CHARS", MIN_HASHED_CLIP_BUDGET_CHARS);
+    const wrappedCommand = buildWrappedCommand({
+        wrapLauncher,
+        shellPath,
+        command,
+        source: "codex",
+        store: shouldStoreFromEnv(),
+        recordStats: false,
+        ...(typeof maxInlineChars === "number" ? { maxInlineChars } : {}),
+    });
+    process.stdout.write(`${JSON.stringify({
+        hookSpecificOutput: {
+            hookEventName: "PreToolUse",
+            permissionDecision: "allow",
+            updatedInput: {
+                command: wrappedCommand,
+            },
+        },
+    })}\n`);
+    return 0;
+}
+function buildCodexExitStatus(exitCode) {
+    return `Process exit code: ${exitCode}.`;
+}
+function buildCodexFeedback(inlineText, rawRefId, exitCode) {
+    return `${inlineText}\n\n${buildCodexExitStatus(exitCode)}\n${buildCompactionHint(rawRefId)}`;
 }
-function buildCodexReplacementOutput(inlineText, rawRefId) {
-    const feedback = buildCodexFeedback(inlineText, rawRefId);
+function buildCodexPostToolUseContext(inlineText, rawRefId, exitCode) {
+    const feedback = buildCodexFeedback(inlineText, rawRefId, exitCode);
     return {
         hookSpecificOutput: {
             hookEventName: "PostToolUse",
@@ -579,11 +645,20 @@ function extractCodexExitCode(payload) {
 }
 export async function installCodexHook(hooksPath = getDefaultHooksPath(), options = {}) {
     const { config, backupPath } = await loadHooksConfig(hooksPath);
-    const command = await buildCodexHookCommand(options);
+    const preCommand = await buildCodexHookCommand(options, "codex-pre-tool-use");
+    const command = preCommand;
+    const preToolUse = config.hooks.PreToolUse ?? [];
+    const retainedPre = preToolUse.filter((group) => !isTokenjuiceCodexHook(group));
+    retainedPre.push(createTokenjuiceCodexHook(preCommand, TOKENJUICE_CODEX_PRE_STATUS));
+    config.hooks.PreToolUse = retainedPre;
     const postToolUse = config.hooks.PostToolUse ?? [];
     const retained = postToolUse.filter((group) => !isTokenjuiceCodexHook(group));
-    retained.push(createTokenjuiceCodexHook(command));
-    config.hooks.PostToolUse = retained;
+    if (retained.length > 0) {
+        config.hooks.PostToolUse = retained;
+    }
+    else {
+        delete config.hooks.PostToolUse;
+    }
     await mkdir(dirname(hooksPath), { recursive: true });
     const tempPath = `${hooksPath}.tmp`;
     await writeFile(tempPath, `${JSON.stringify(config, null, 2)}\n`, "utf8");
@@ -593,14 +668,23 @@ export async function installCodexHook(hooksPath = getDefaultHooksPath(), option
         hooksPath,
         ...(backupPath ? { backupPath } : {}),
         command,
+        preCommand,
         featureFlag,
     };
 }
 export async function uninstallCodexHook(hooksPath = getDefaultHooksPath()) {
     const { config, backupPath } = await loadHooksConfig(hooksPath);
+    const preToolUse = config.hooks.PreToolUse ?? [];
+    const retainedPre = preToolUse.filter((group) => !isTokenjuiceCodexHook(group));
     const postToolUse = config.hooks.PostToolUse ?? [];
     const retained = postToolUse.filter((group) => !isTokenjuiceCodexHook(group));
-    const removed = postToolUse.length - retained.length;
+    const removed = preToolUse.length - retainedPre.length + postToolUse.length - retained.length;
+    if (retainedPre.length > 0) {
+        config.hooks.PreToolUse = retainedPre;
+    }
+    else {
+        delete config.hooks.PreToolUse;
+    }
     if (retained.length > 0) {
         config.hooks.PostToolUse = retained;
     }
@@ -618,11 +702,13 @@ export async function uninstallCodexHook(hooksPath = getDefaultHooksPath()) {
     };
 }
 export async function doctorCodexHook(hooksPath = getDefaultHooksPath(), options = {}) {
-    const expectedCommand = await buildCodexHookCommand(options);
+    const expectedPreCommand = await buildCodexHookCommand(options, "codex-pre-tool-use");
+    const expectedCommand = expectedPreCommand;
     const installFixCommand = getCodexFixCommand(options.local);
     let fixCommand = installFixCommand;
     const { config, exists } = await readHooksConfig(hooksPath);
     const detectedCommand = findTokenjuiceCodexHookCommand(config);
+    const detectedPreCommand = findTokenjuiceCodexPreHookCommand(config);
     const featureFlag = await inspectCodexHooksFeatureFlag(options.featureFlagConfigPath);
     const runtimeConfig = await inspectCodexRuntimeConfig(options.featureFlagConfigPath);
     if (!exists) {
@@ -632,26 +718,30 @@ export async function doctorCodexHook(hooksPath = getDefaultHooksPath(), options
             issues: [],
             fixCommand,
             expectedCommand,
+            expectedPreCommand,
             checkedPaths: [],
             missingPaths: [],
             featureFlag,
             runtimeConfig,
         };
     }
-    if (!detectedCommand) {
+    if (!detectedPreCommand && !detectedCommand) {
         return {
             hooksPath,
             status: "disabled",
             issues: [],
             fixCommand,
             expectedCommand,
+            expectedPreCommand,
             checkedPaths: [],
             missingPaths: [],
             featureFlag,
             runtimeConfig,
         };
     }
-    const checkedPaths = extractHookCommandPaths(detectedCommand);
+    const checkedPaths = [...new Set([detectedCommand, detectedPreCommand]
+            .filter((command) => command !== undefined)
+            .flatMap((command) => extractHookCommandPaths(command)))];
     const missingPaths = [];
     for (const path of checkedPaths) {
         if (!(await isExecutableFile(path)) && !(path.endsWith(".js") && await pathExists(path))) {
@@ -659,12 +749,18 @@ export async function doctorCodexHook(hooksPath = getDefaultHooksPath(), options
         }
     }
     const issues = [];
-    if (detectedCommand !== expectedCommand) {
-        if (detectedCommand.includes("/Cellar/")) {
+    if (!detectedPreCommand) {
+        issues.push("Codex tokenjuice PreToolUse wrapping hook is missing");
+        if (detectedCommand?.includes("/Cellar/")) {
+            issues.push("configured Codex hook is pinned to a versioned Homebrew Cellar path");
+        }
+    }
+    else if (detectedPreCommand !== expectedPreCommand) {
+        if (detectedPreCommand.includes("/Cellar/")) {
             issues.push("configured Codex hook is pinned to a versioned Homebrew Cellar path");
         }
         else {
-            issues.push("configured Codex hook command does not match the current recommended command");
+            issues.push("configured Codex tokenjuice PreToolUse command does not match the current recommended command");
         }
     }
     if (missingPaths.length > 0) {
@@ -690,20 +786,22 @@ export async function doctorCodexHook(hooksPath = getDefaultHooksPath(), options
         issues,
         fixCommand,
         expectedCommand,
-        detectedCommand,
+        expectedPreCommand,
+        ...(detectedCommand ? { detectedCommand } : {}),
+        ...(detectedPreCommand ? { detectedPreCommand } : {}),
         checkedPaths,
         missingPaths,
         featureFlag,
         runtimeConfig,
     };
 }
-function readPositiveIntegerEnv(name) {
+function readIntegerEnvAtLeast(name, minimum) {
     const value = process.env[name];
     if (!value) {
         return undefined;
     }
     const parsed = Number(value);
-    return Number.isInteger(parsed) && parsed > 0 ? parsed : undefined;
+    return Number.isInteger(parsed) && parsed >= minimum ? parsed : undefined;
 }
 function shouldStoreFromEnv() {
     const value = process.env.TOKENJUICE_CODEX_STORE;
@@ -840,11 +938,14 @@ export async function runCodexPostToolUseHook(rawText) {
     catch {
         return 0;
     }
-    const command = payload.tool_input?.command;
+    if (payload.hook_event_name === "PreToolUse") {
+        return runCodexPreToolUseHook(rawText);
+    }
+    const wireCommand = payload.tool_input?.command;
     const debug = {
         hookEvent: payload.hook_event_name,
         toolName: payload.tool_name,
-        command,
+        command: wireCommand,
         rewrote: false,
     };
     if (payload.hook_event_name !== "PostToolUse") {
@@ -855,16 +956,22 @@ export async function runCodexPostToolUseHook(rawText) {
         await writeHookDebug({ ...debug, skipped: "non-bash" });
         return 0;
     }
-    if (typeof command !== "string" || !command.trim()) {
+    if (typeof wireCommand !== "string" || !wireCommand.trim()) {
         await writeHookDebug({ ...debug, skipped: "missing-command" });
         return 0;
     }
+    const command = wireCommand;
     const combinedText = stringifyToolResponse(payload.tool_response);
+    const exitCode = extractCodexExitCode(payload);
+    debug.command = command;
+    if (exitCode === undefined) {
+        await writeHookDebug({ ...debug, skipped: "missing-exit-status" });
+        return 0;
+    }
     if (!combinedText.trim()) {
         await writeHookDebug({ ...debug, skipped: "empty-tool-response" });
         return 0;
     }
-    const exitCode = extractCodexExitCode(payload);
     const executionInput = {
         toolName: "exec",
         command,
@@ -886,18 +993,8 @@ export async function runCodexPostToolUseHook(rawText) {
         });
         return 0;
     }
-    const inspectionSkipReason = getOutputAwareInspectionSkipReason("allow-safe-inventory", executionInput);
-    if (inspectionSkipReason) {
-        await recordImmediateHookStats(executionInput, combinedText, storeRaw);
-        const stats = buildImmediateSkipStats(combinedText);
-        await writeHookDebug({
-            ...debug,
-            ...stats,
-            skipped: inspectionSkipReason,
-        });
-        return 0;
-    }
-    const maxInlineChars = readPositiveIntegerEnv("TOKENJUICE_CODEX_MAX_INLINE_CHARS");
+    const maxInlineChars = readIntegerEnvAtLeast("TOKENJUICE_CODEX_MAX_INLINE_CHARS", MIN_HASHED_CLIP_BUDGET_CHARS);
+    const boundedMaxInlineChars = maxInlineChars ?? DEFAULT_CODEX_MAX_INLINE_CHARS;
     try {
         const outcome = await compactBashResult({
             source: "codex",
@@ -906,6 +1003,10 @@ export async function runCodexPostToolUseHook(rawText) {
             ...(typeof payload.cwd === "string" && payload.cwd.trim() ? { cwd: payload.cwd } : {}),
             ...(typeof exitCode === "number" ? { exitCode } : {}),
             ...(typeof maxInlineChars === "number" ? { maxInlineChars } : {}),
+            boundedKeep: {
+                maxInlineChars: boundedMaxInlineChars,
+            },
+            inspectionPolicy: "allow-safe-inventory",
             storeRaw,
             metadata: {
                 source: "codex-post-tool-use",
@@ -915,7 +1016,7 @@ export async function runCodexPostToolUseHook(rawText) {
             genericFallbackMaxRatio: GENERIC_FALLBACK_MAX_RATIO,
             skipGenericFallbackForCompoundCommands: true,
         });
-        const result = outcome.action === "rewrite" ? outcome.result : outcome.result;
+        const result = outcome.result;
         if (result) {
             const rawChars = result.stats.rawChars;
             const reducedChars = result.stats.reducedChars;
@@ -926,11 +1027,18 @@ export async function runCodexPostToolUseHook(rawText) {
             debug.ratio = result.stats.ratio;
             debug.matchedReducer = result.classification.matchedReducer;
         }
+        if (outcome.action === "rewrite" && outcome.fallbackReason) {
+            debug.fallbackReason = outcome.fallbackReason;
+        }
         if (outcome.action === "keep") {
+            if (!result) {
+                await recordImmediateHookStats(executionInput, combinedText, storeRaw);
+                Object.assign(debug, buildImmediateSkipStats(combinedText));
+            }
             await writeHookDebug({ ...debug, skipped: outcome.reason });
             return 0;
         }
-        process.stdout.write(`${JSON.stringify(buildCodexReplacementOutput(outcome.result.inlineText, outcome.result.rawRef?.id))}\n`);
+        process.stdout.write(`${JSON.stringify(buildCodexPostToolUseContext(outcome.result.inlineText, outcome.result.rawRef?.id, exitCode))}\n`);
         await writeHookDebug({ ...debug, rewrote: true });
         return 0;
     }
diff --git a/dist/hosts/openclaw/extension.js b/dist/hosts/openclaw/extension.js
index 11d3c3e8d4d4c444ac137635b55739529841eb58..5a010e69981524a210073c9a0ca20e5f8d7ab3e4 100644
--- a/dist/hosts/openclaw/extension.js
+++ b/dist/hosts/openclaw/extension.js
@@ -1,4 +1,4 @@
-import { compactBashResult, getOutputAwareInspectionSkipReason } from "../../core/integrations/compact-bash-result.js";
+import { compactBashResult } from "../../core/integrations/compact-bash-result.js";
 import { buildCompactionNotice, buildTokenjuiceDetails, extractTextContent, mergeDetails, } from "../shared/tool-result.js";
 import { formatErrorMessage } from "../pi/extension/utils.js";
 const DEFAULT_MAX_INLINE_CHARS = 1200;
@@ -58,14 +58,6 @@ export function createTokenjuiceOpenClawEmbeddedExtension() {
             if (!outputText.trim()) {
                 return undefined;
             }
-            const executionInput = {
-                toolName: "exec",
-                command,
-                combinedText: outputText,
-            };
-            if (getOutputAwareInspectionSkipReason("allow-safe-inventory", executionInput)) {
-                return undefined;
-            }
             try {
                 const outcome = await compactBashResult({
                     source: "openclaw",
@@ -74,6 +66,9 @@ export function createTokenjuiceOpenClawEmbeddedExtension() {
                     visibleText: outputText,
                     exitCode: readExitCode(event.details, Boolean(event.isError)),
                     maxInlineChars: DEFAULT_MAX_INLINE_CHARS,
+                    boundedKeep: {
+                        maxInlineChars: DEFAULT_MAX_INLINE_CHARS,
+                    },
                     inspectionPolicy: "allow-safe-inventory",
                     minSavedCharsAny: 8,
                     genericFallbackMinSavedChars: GENERIC_FALLBACK_MIN_SAVED_CHARS,
@@ -93,7 +88,7 @@ export function createTokenjuiceOpenClawEmbeddedExtension() {
                             text: `${outcome.result.inlineText}\n\n[${buildCompactionNotice(outcome.result)}]`,
                         },
                     ],
-                    details: mergeDetails(event.details, buildTokenjuiceDetails(outcome.result)),
+                    details: mergeDetails(event.details, buildTokenjuiceDetails(outcome.result, outcome.fallbackReason)),
                 };
             }
             catch (error) {
diff --git a/dist/hosts/opencode/extension/runtime.js b/dist/hosts/opencode/extension/runtime.js
index c56003c6d218cac1cfe61aac515741d9e30647fb..6720d4c9123ec00ed40da3bfe149932a201e49c6 100644
--- a/dist/hosts/opencode/extension/runtime.js
+++ b/dist/hosts/opencode/extension/runtime.js
@@ -1283,6 +1283,12 @@ function readNoOmissionFromEnv(env = process.env) {
   return NO_OMISSION_TRUTHY_VALUES.has(env[NO_OMISSION_ENV] ?? "");
 }
 
+// src/core/artifacts.ts
+import { mkdir, readFile, readdir, writeFile } from "node:fs/promises";
+import { homedir } from "node:os";
+import { join } from "node:path";
+import { randomUUID } from "node:crypto";
+
 // src/core/compaction-metadata.ts
 var NO_COMPACTION_METADATA = {
   authoritative: false,
@@ -1312,9 +1318,6 @@ function mergeCompactionMetadata(...values) {
   return buildCompactionMetadata(presentValues.every((value) => value.authoritative), ...kinds);
 }
 
-// src/core/reduce-utils.ts
-import { createHash } from "node:crypto";
-
 // src/core/text.ts
 var ANSI_CSI_PATTERN = new RegExp(String.raw`\u001B\[[0-?]*[ -/]*[@-~]`, "g");
 var ANSI_OSC_PATTERN = new RegExp(String.raw`\u001B\][^\u0007\u001B]*(?:\u0007|\u001B\\)`, "g");
@@ -1463,15 +1466,214 @@ function pluralize(count, noun) {
   return `${count} ${noun}s`;
 }
 
+// src/core/source.ts
+var DEFAULT_CLI_ARTIFACT_SOURCE = "cli";
+function normalizeArtifactSource(value) {
+  if (typeof value !== "string") {
+    return null;
+  }
+  const source = value.trim().toLowerCase().replace(/[_\s]+/gu, "-").replace(/[^a-z0-9.-]+/gu, "-").replace(/^-+|-+$/gu, "");
+  if (!source) {
+    return null;
+  }
+  if (source.startsWith("amazon-q") || source.startsWith("amazonq") || source.startsWith("aws-q")) {
+    return "amazon-q";
+  }
+  if (source.startsWith("claude-code") || source === "claude") {
+    return "claude-code";
+  }
+  if (source.startsWith("cline")) {
+    return "cline";
+  }
+  if (source.startsWith("codex")) {
+    return "codex";
+  }
+  if (source.startsWith("command-code") || source.startsWith("commandcode")) {
+    return "command-code";
+  }
+  if (source.startsWith("copilot-agent") || source.startsWith("github-copilot-agent")) {
+    return "copilot-agent";
+  }
+  if (source.startsWith("copilot-cli") || source.startsWith("github-copilot-cli")) {
+    return "copilot-cli";
+  }
+  if (source.startsWith("crush")) {
+    return "crush";
+  }
+  if (source.startsWith("cursor")) {
+    return "cursor";
+  }
+  if (source.startsWith("droid") || source.startsWith("factory-droid")) {
+    return "droid";
+  }
+  if (source.startsWith("gemini")) {
+    return "gemini-cli";
+  }
+  if (source.startsWith("grok-build") || source.startsWith("grokbuild") || source.startsWith("xai-grok-build")) {
+    return "grok-build";
+  }
+  if (source.startsWith("grok")) {
+    return "grok-cli";
+  }
+  if (source.startsWith("goose")) {
+    return "goose";
+  }
+  if (source.startsWith("antigravity") || source === "google-antigravity") {
+    return "antigravity";
+  }
+  if (source.startsWith("augment") || source.startsWith("auggie")) {
+    return "augment";
+  }
+  if (source.startsWith("openclaw")) {
+    return "openclaw";
+  }
+  if (source.startsWith("openhands")) {
+    return "openhands";
+  }
+  if (source.startsWith("openinterpreter") || source.startsWith("open-interpreter")) {
+    return "open-interpreter";
+  }
+  if (source.startsWith("openwebui") || source.startsWith("open-webui")) {
+    return "openwebui";
+  }
+  if (source.startsWith("opencode") || source.startsWith("open-code")) {
+    return "opencode";
+  }
+  if (source.startsWith("pi")) {
+    return "pi";
+  }
+  if (source.startsWith("plandex")) {
+    return "plandex";
+  }
+  if (source.startsWith("qoder")) {
+    return "qoder";
+  }
+  if (source.startsWith("qwen")) {
+    return "qwen-code";
+  }
+  if (source.startsWith("kimi")) {
+    return "kimi";
+  }
+  if (source.startsWith("mux")) {
+    return "mux";
+  }
+  if (source.startsWith("ruler")) {
+    return "ruler";
+  }
+  if (source.startsWith("vscode-copilot") || source.startsWith("vs-code-copilot")) {
+    return "vscode-copilot";
+  }
+  if (source === "direct" || source === "tokenjuice" || source === "wrap") {
+    return DEFAULT_CLI_ARTIFACT_SOURCE;
+  }
+  return source;
+}
+function resolveArtifactSource(input) {
+  return normalizeArtifactSource(input.metadata?.source) ?? DEFAULT_CLI_ARTIFACT_SOURCE;
+}
+
+// src/core/artifacts.ts
+var ARTIFACT_ID_PATTERN = /^tj_[0-9a-f-]{12}$/iu;
+var ARTIFACT_DIR_ENV = "TOKENJUICE_ARTIFACT_DIR";
+function extractCaptureTruncatedFlag(input) {
+  const value = input.metadata?.tokenjuiceCaptureTruncated;
+  return typeof value === "boolean" ? value : void 0;
+}
+function getDefaultArtifactDir() {
+  const artifactDir = process.env[ARTIFACT_DIR_ENV];
+  if (typeof artifactDir === "string" && artifactDir.trim()) {
+    return artifactDir.trim();
+  }
+  return join(homedir(), ".tokenjuice", "artifacts");
+}
+function resolveArtifactBaseDir(storeDir) {
+  return storeDir ?? getDefaultArtifactDir();
+}
+function isValidArtifactId(id) {
+  return ARTIFACT_ID_PATTERN.test(id);
+}
+function buildArtifactPaths(id, storeDir) {
+  if (!isValidArtifactId(id)) {
+    throw new Error(`invalid artifact id: ${id}`);
+  }
+  const base = resolveArtifactBaseDir(storeDir);
+  return {
+    id,
+    storage: "file",
+    path: join(base, `${id}.txt`),
+    metadataPath: join(base, `${id}.json`)
+  };
+}
+function buildMetadataOnlyPath(id, storeDir) {
+  if (!isValidArtifactId(id)) {
+    throw new Error(`invalid artifact id: ${id}`);
+  }
+  return join(resolveArtifactBaseDir(storeDir), `${id}.meta.json`);
+}
+async function storeArtifact(input, storeDir) {
+  const id = `tj_${randomUUID().slice(0, 12)}`;
+  const ref = buildArtifactPaths(id, storeDir);
+  await mkdir(resolveArtifactBaseDir(storeDir), { recursive: true, mode: 448 });
+  const captureTruncated = extractCaptureTruncatedFlag(input.input);
+  const artifact = {
+    id,
+    rawText: input.rawText,
+    metadata: {
+      createdAt: (/* @__PURE__ */ new Date()).toISOString(),
+      source: resolveArtifactSource(input.input),
+      classification: input.classification,
+      rawChars: input.stats?.rawChars ?? countTextChars(stripAnsi(input.rawText)),
+      ...input.input.toolName ? { toolName: input.input.toolName } : {},
+      ...input.input.command ? { command: input.input.command } : {},
+      ...typeof input.input.exitCode === "number" ? { exitCode: input.input.exitCode } : {},
+      ...captureTruncated !== void 0 ? { captureTruncated } : {},
+      ...input.stats ? { reducedChars: input.stats.reducedChars, ratio: input.stats.ratio } : {}
+    }
+  };
+  await Promise.all([
+    writeFile(ref.path, input.rawText, { encoding: "utf8", mode: 384 }),
+    writeFile(ref.metadataPath, JSON.stringify(artifact.metadata, null, 2), { encoding: "utf8", mode: 384 })
+  ]);
+  return ref;
+}
+async function storeArtifactMetadata(input, storeDir) {
+  const id = `tj_${randomUUID().slice(0, 12)}`;
+  const metadataPath = buildMetadataOnlyPath(id, storeDir);
+  const captureTruncated = extractCaptureTruncatedFlag(input.input);
+  const metadata = {
+    createdAt: (/* @__PURE__ */ new Date()).toISOString(),
+    source: resolveArtifactSource(input.input),
+    classification: input.classification,
+    rawChars: input.stats?.rawChars ?? countTextChars(stripAnsi(input.rawText)),
+    ...input.input.toolName ? { toolName: input.input.toolName } : {},
+    ...input.input.command ? { command: input.input.command } : {},
+    ...typeof input.input.exitCode === "number" ? { exitCode: input.input.exitCode } : {},
+    ...captureTruncated !== void 0 ? { captureTruncated } : {},
+    ...input.stats ? { reducedChars: input.stats.reducedChars, ratio: input.stats.ratio } : {}
+  };
+  await mkdir(resolveArtifactBaseDir(storeDir), { recursive: true, mode: 448 });
+  await writeFile(metadataPath, JSON.stringify(metadata, null, 2), { encoding: "utf8", mode: 384 });
+  return {
+    id,
+    storage: "file",
+    metadataPath,
+    metadata
+  };
+}
+
 // src/core/reduce-utils.ts
+import { createHash } from "node:crypto";
 function compactWhitespace(text) {
   return text.replace(/\s+/gu, " ").trim();
 }
 function shortHash(text) {
   return createHash("sha256").update(text).digest("hex").slice(0, 12);
 }
+var MIN_HASHED_CLIP_BUDGET_CHARS = 64;
 function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
-  if (countTextChars(text) <= maxChars) {
+  const boundedMaxChars = Math.max(0, Math.floor(maxChars));
+  const inputChars = countTextChars(text);
+  if (inputChars <= boundedMaxChars) {
     return { text };
   }
   if (noOmit) {
@@ -1480,16 +1682,27 @@ function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
       compaction: createPassthroughCompactionMetadata("no-omit-char-clip-passthrough")
     };
   }
-  const omitted = countTextChars(text) - maxChars;
-  const marker = `...[${omitted} chars omitted, sha256:${shortHash(text)}]...`;
-  const markerChars = countTextChars(marker);
-  if (maxChars <= markerChars) {
+  const buildMarker = (omitted2) => `...[${omitted2} chars omitted, sha256:${shortHash(text)}]...`;
+  let omitted = inputChars;
+  let marker = buildMarker(omitted);
+  let markerChars = countTextChars(marker);
+  if (boundedMaxChars <= markerChars) {
     return {
-      text: sliceTextChars(marker, 0, maxChars),
+      text: sliceTextChars(marker, 0, boundedMaxChars),
       compaction: createCompactionMetadata("hashed-middle-clip")
     };
   }
-  const bodyChars = maxChars - markerChars;
+  let bodyChars = boundedMaxChars - markerChars;
+  for (; ; ) {
+    const nextOmitted = inputChars - bodyChars;
+    if (nextOmitted === omitted) {
+      break;
+    }
+    omitted = nextOmitted;
+    marker = buildMarker(omitted);
+    markerChars = countTextChars(marker);
+    bodyChars = Math.max(0, boundedMaxChars - markerChars);
+  }
   const headChars = Math.ceil(bodyChars * 0.7);
   const tailChars = Math.max(0, bodyChars - headChars);
   return {
@@ -1497,6 +1710,12 @@ function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
     compaction: createCompactionMetadata("hashed-middle-clip")
   };
 }
+function clipMiddleWithHashToBudget(text, maxChars) {
+  if (!Number.isInteger(maxChars) || maxChars < MIN_HASHED_CLIP_BUDGET_CHARS) {
+    return { text };
+  }
+  return clipMiddleWithHashStrict(text, maxChars);
+}
 function minifyJsonLexically(rawText) {
   const text = rawText.trim();
   let output = "";
@@ -1687,9 +1906,9 @@ function buildInspectionSummary(input, rawText, noOmit = false) {
 }
 
 // src/core/rules.ts
-import { readdir, readFile, realpath } from "node:fs/promises";
-import { homedir } from "node:os";
-import { isAbsolute, join, relative, resolve } from "node:path";
+import { readdir as readdir2, readFile as readFile2, realpath } from "node:fs/promises";
+import { homedir as homedir2 } from "node:os";
+import { isAbsolute, join as join2, relative, resolve } from "node:path";
 import { fileURLToPath } from "node:url";
 
 // src/rules/archive/tar.json
@@ -7254,10 +7473,10 @@ async function listRuleFiles(root) {
   }
   const rootRealPath = resolvedRoot;
   async function walk(currentDir) {
-    const entries = (await readdir(currentDir, { withFileTypes: true })).sort((left, right) => left.name.localeCompare(right.name));
+    const entries = (await readdir2(currentDir, { withFileTypes: true })).sort((left, right) => left.name.localeCompare(right.name));
     const files = await Promise.all(
       entries.map(async (entry) => {
-        const fullPath = join(currentDir, entry.name);
+        const fullPath = join2(currentDir, entry.name);
         if (entry.isSymbolicLink()) {
           return [];
         }
@@ -7287,10 +7506,10 @@ async function listRuleFiles(root) {
   }
 }
 function userRulesRoot(customDir) {
-  return customDir ?? join(homedir(), ".config", "tokenjuice", "rules");
+  return customDir ?? join2(homedir2(), ".config", "tokenjuice", "rules");
 }
 function projectRulesRoot(cwd, customDir) {
-  return customDir ?? join(cwd ?? process.cwd(), ".tokenjuice", "rules");
+  return customDir ?? join2(cwd ?? process.cwd(), ".tokenjuice", "rules");
 }
 function sortRules(rules) {
   return [...rules].sort((left, right) => {
@@ -7307,7 +7526,7 @@ async function loadRuleDescriptorsFromRoot(root, source) {
   const files = await listRuleFiles(root);
   return await Promise.all(
     files.map(async (fullPath) => {
-      const parsed = JSON.parse(await readFile(fullPath, "utf8"));
+      const parsed = JSON.parse(await readFile2(fullPath, "utf8"));
       assertValidRule(parsed);
       return {
         source,
@@ -7590,207 +7809,6 @@ function normalizeExecutionInput(input) {
   };
 }
 
-// src/core/artifacts.ts
-import { mkdir, readFile as readFile2, readdir as readdir2, writeFile } from "node:fs/promises";
-import { homedir as homedir2 } from "node:os";
-import { join as join2 } from "node:path";
-import { randomUUID } from "node:crypto";
-
-// src/core/source.ts
-var DEFAULT_CLI_ARTIFACT_SOURCE = "cli";
-function normalizeArtifactSource(value) {
-  if (typeof value !== "string") {
-    return null;
-  }
-  const source = value.trim().toLowerCase().replace(/[_\s]+/gu, "-").replace(/[^a-z0-9.-]+/gu, "-").replace(/^-+|-+$/gu, "");
-  if (!source) {
-    return null;
-  }
-  if (source.startsWith("amazon-q") || source.startsWith("amazonq") || source.startsWith("aws-q")) {
-    return "amazon-q";
-  }
-  if (source.startsWith("claude-code") || source === "claude") {
-    return "claude-code";
-  }
-  if (source.startsWith("cline")) {
-    return "cline";
-  }
-  if (source.startsWith("codex")) {
-    return "codex";
-  }
-  if (source.startsWith("command-code") || source.startsWith("commandcode")) {
-    return "command-code";
-  }
-  if (source.startsWith("copilot-agent") || source.startsWith("github-copilot-agent")) {
-    return "copilot-agent";
-  }
-  if (source.startsWith("copilot-cli") || source.startsWith("github-copilot-cli")) {
-    return "copilot-cli";
-  }
-  if (source.startsWith("crush")) {
-    return "crush";
-  }
-  if (source.startsWith("cursor")) {
-    return "cursor";
-  }
-  if (source.startsWith("droid") || source.startsWith("factory-droid")) {
-    return "droid";
-  }
-  if (source.startsWith("gemini")) {
-    return "gemini-cli";
-  }
-  if (source.startsWith("grok-build") || source.startsWith("grokbuild") || source.startsWith("xai-grok-build")) {
-    return "grok-build";
-  }
-  if (source.startsWith("grok")) {
-    return "grok-cli";
-  }
-  if (source.startsWith("goose")) {
-    return "goose";
-  }
-  if (source.startsWith("antigravity") || source === "google-antigravity") {
-    return "antigravity";
-  }
-  if (source.startsWith("augment") || source.startsWith("auggie")) {
-    return "augment";
-  }
-  if (source.startsWith("openclaw")) {
-    return "openclaw";
-  }
-  if (source.startsWith("openhands")) {
-    return "openhands";
-  }
-  if (source.startsWith("openinterpreter") || source.startsWith("open-interpreter")) {
-    return "open-interpreter";
-  }
-  if (source.startsWith("openwebui") || source.startsWith("open-webui")) {
-    return "openwebui";
-  }
-  if (source.startsWith("opencode") || source.startsWith("open-code")) {
-    return "opencode";
-  }
-  if (source.startsWith("pi")) {
-    return "pi";
-  }
-  if (source.startsWith("plandex")) {
-    return "plandex";
-  }
-  if (source.startsWith("qoder")) {
-    return "qoder";
-  }
-  if (source.startsWith("qwen")) {
-    return "qwen-code";
-  }
-  if (source.startsWith("kimi")) {
-    return "kimi";
-  }
-  if (source.startsWith("mux")) {
-    return "mux";
-  }
-  if (source.startsWith("ruler")) {
-    return "ruler";
-  }
-  if (source.startsWith("vscode-copilot") || source.startsWith("vs-code-copilot")) {
-    return "vscode-copilot";
-  }
-  if (source === "direct" || source === "tokenjuice" || source === "wrap") {
-    return DEFAULT_CLI_ARTIFACT_SOURCE;
-  }
-  return source;
-}
-function resolveArtifactSource(input) {
-  return normalizeArtifactSource(input.metadata?.source) ?? DEFAULT_CLI_ARTIFACT_SOURCE;
-}
-
-// src/core/artifacts.ts
-var ARTIFACT_ID_PATTERN = /^tj_[0-9a-f-]{12}$/iu;
-var ARTIFACT_DIR_ENV = "TOKENJUICE_ARTIFACT_DIR";
-function extractCaptureTruncatedFlag(input) {
-  const value = input.metadata?.tokenjuiceCaptureTruncated;
-  return typeof value === "boolean" ? value : void 0;
-}
-function getDefaultArtifactDir() {
-  const artifactDir = process.env[ARTIFACT_DIR_ENV];
-  if (typeof artifactDir === "string" && artifactDir.trim()) {
-    return artifactDir.trim();
-  }
-  return join2(homedir2(), ".tokenjuice", "artifacts");
-}
-function resolveArtifactBaseDir(storeDir) {
-  return storeDir ?? getDefaultArtifactDir();
-}
-function isValidArtifactId(id) {
-  return ARTIFACT_ID_PATTERN.test(id);
-}
-function buildArtifactPaths(id, storeDir) {
-  if (!isValidArtifactId(id)) {
-    throw new Error(`invalid artifact id: ${id}`);
-  }
-  const base = resolveArtifactBaseDir(storeDir);
-  return {
-    id,
-    storage: "file",
-    path: join2(base, `${id}.txt`),
-    metadataPath: join2(base, `${id}.json`)
-  };
-}
-function buildMetadataOnlyPath(id, storeDir) {
-  if (!isValidArtifactId(id)) {
-    throw new Error(`invalid artifact id: ${id}`);
-  }
-  return join2(resolveArtifactBaseDir(storeDir), `${id}.meta.json`);
-}
-async function storeArtifact(input, storeDir) {
-  const id = `tj_${randomUUID().slice(0, 12)}`;
-  const ref = buildArtifactPaths(id, storeDir);
-  await mkdir(resolveArtifactBaseDir(storeDir), { recursive: true, mode: 448 });
-  const captureTruncated = extractCaptureTruncatedFlag(input.input);
-  const artifact = {
-    id,
-    rawText: input.rawText,
-    metadata: {
-      createdAt: (/* @__PURE__ */ new Date()).toISOString(),
-      source: resolveArtifactSource(input.input),
-      classification: input.classification,
-      rawChars: input.stats?.rawChars ?? countTextChars(stripAnsi(input.rawText)),
-      ...input.input.toolName ? { toolName: input.input.toolName } : {},
-      ...input.input.command ? { command: input.input.command } : {},
-      ...typeof input.input.exitCode === "number" ? { exitCode: input.input.exitCode } : {},
-      ...captureTruncated !== void 0 ? { captureTruncated } : {},
-      ...input.stats ? { reducedChars: input.stats.reducedChars, ratio: input.stats.ratio } : {}
-    }
-  };
-  await Promise.all([
-    writeFile(ref.path, input.rawText, { encoding: "utf8", mode: 384 }),
-    writeFile(ref.metadataPath, JSON.stringify(artifact.metadata, null, 2), { encoding: "utf8", mode: 384 })
-  ]);
-  return ref;
-}
-async function storeArtifactMetadata(input, storeDir) {
-  const id = `tj_${randomUUID().slice(0, 12)}`;
-  const metadataPath = buildMetadataOnlyPath(id, storeDir);
-  const captureTruncated = extractCaptureTruncatedFlag(input.input);
-  const metadata = {
-    createdAt: (/* @__PURE__ */ new Date()).toISOString(),
-    source: resolveArtifactSource(input.input),
-    classification: input.classification,
-    rawChars: input.stats?.rawChars ?? countTextChars(stripAnsi(input.rawText)),
-    ...input.input.toolName ? { toolName: input.input.toolName } : {},
-    ...input.input.command ? { command: input.input.command } : {},
-    ...typeof input.input.exitCode === "number" ? { exitCode: input.input.exitCode } : {},
-    ...captureTruncated !== void 0 ? { captureTruncated } : {},
-    ...input.stats ? { reducedChars: input.stats.reducedChars, ratio: input.stats.ratio } : {}
-  };
-  await mkdir(resolveArtifactBaseDir(storeDir), { recursive: true, mode: 448 });
-  await writeFile(metadataPath, JSON.stringify(metadata, null, 2), { encoding: "utf8", mode: 384 });
-  return {
-    id,
-    storage: "file",
-    metadataPath,
-    metadata
-  };
-}
-
 // src/core/github-actions-summary.ts
 var GITHUB_ACTIONS_EXIT_PATTERN = /^Error: Process completed with exit code (\d+)\.?$/u;
 var GITHUB_ACTIONS_RUN_PATTERN = /^(?:##\[group\]|::group::)?Run\s+(.+)$/u;
@@ -8991,6 +9009,59 @@ function getCompactionSkipReason(command, rawText, result, options) {
 }
 
 // src/core/integrations/compact-bash-result.ts
+function buildBoundedFallbackResult(rawText, result, clipped) {
+  const rawChars = result?.stats.rawChars ?? countTextChars(rawText);
+  const reducedChars = countTextChars(clipped.text);
+  return {
+    ...result ?? {
+      classification: {
+        family: "preserved-output",
+        confidence: 0
+      }
+    },
+    inlineText: clipped.text,
+    ...clipped.compaction ? { compaction: mergeCompactionMetadata(result?.compaction, clipped.compaction) } : {},
+    stats: {
+      rawChars,
+      reducedChars,
+      ratio: rawChars === 0 ? 1 : reducedChars / rawChars
+    }
+  };
+}
+async function applyBoundedKeepFallback(input, keep, executionInput) {
+  const maxInlineChars = input.boundedKeep?.maxInlineChars;
+  if (typeof maxInlineChars !== "number" || !Number.isInteger(maxInlineChars) || maxInlineChars < MIN_HASHED_CLIP_BUDGET_CHARS) {
+    return keep;
+  }
+  const clipped = clipMiddleWithHashToBudget(keep.rawText, maxInlineChars);
+  if (clipped.text === keep.rawText) {
+    return keep;
+  }
+  let result = buildBoundedFallbackResult(keep.rawText, keep.result, clipped);
+  if (!keep.result) {
+    const artifactInput = {
+      input: executionInput,
+      rawText: keep.rawText,
+      classification: result.classification,
+      stats: result.stats
+    };
+    if (input.storeRaw) {
+      result = {
+        ...result,
+        rawRef: await storeArtifact(artifactInput)
+      };
+    } else {
+      await storeArtifactMetadata(artifactInput);
+    }
+  }
+  return {
+    action: "rewrite",
+    rawText: keep.rawText,
+    usedTrustedFullText: keep.usedTrustedFullText,
+    result,
+    fallbackReason: keep.reason
+  };
+}
 function resolveInspectionPolicy(input) {
   if (input.inspectionPolicy) {
     return input.inspectionPolicy;
@@ -9038,12 +9109,12 @@ async function compactBashResult(input) {
   };
   const inspectionSkipReason = getOutputAwareInspectionSkipReason(resolveInspectionPolicy(input), executionInput);
   if (inspectionSkipReason) {
-    return {
+    return applyBoundedKeepFallback(input, {
       action: "keep",
       reason: inspectionSkipReason,
       rawText,
       usedTrustedFullText
-    };
+    }, executionInput);
   }
   const options = {
     ...typeof input.cwd === "string" && input.cwd.trim() ? { cwd: input.cwd } : {},
@@ -9055,13 +9126,13 @@ async function compactBashResult(input) {
   const result = await reduceExecution(executionInput, options);
   const skipReason = getCompactionSkipReason(command, rawText, result, input);
   if (skipReason) {
-    return {
+    return applyBoundedKeepFallback(input, {
       action: "keep",
       reason: skipReason,
       rawText,
       usedTrustedFullText,
       result
-    };
+    }, executionInput);
   }
   return {
     action: "rewrite",
@@ -9083,7 +9154,7 @@ function buildCompactionNotice(result, fullOutputPath) {
   const suffix = hints.length > 0 ? ` (${hints.join(", ")})` : "";
   return `tokenjuice compacted bash output${suffix}`;
 }
-function buildTokenjuiceDetails(result) {
+function buildTokenjuiceDetails(result, fallbackReason) {
   const rawChars = typeof result.stats?.rawChars === "number" ? result.stats.rawChars : 0;
   const reducedChars = typeof result.stats?.reducedChars === "number" ? result.stats.reducedChars : 0;
   return {
@@ -9091,7 +9162,8 @@ function buildTokenjuiceDetails(result) {
     rawChars,
     reducedChars,
     savedChars: Math.max(0, rawChars - reducedChars),
-    ...typeof result.classification?.matchedReducer === "string" ? { reducer: result.classification.matchedReducer } : {}
+    ...typeof result.classification?.matchedReducer === "string" ? { reducer: result.classification.matchedReducer } : {},
+    ...fallbackReason ? { fallbackReason } : {}
   };
 }
 
diff --git a/dist/hosts/pi/extension/runtime.js b/dist/hosts/pi/extension/runtime.js
index f3383d28a584a2870621c3e9ea442aa9a9bfca1e..041bd4dabe7e4f2ac4bbf054693c58a48d77cc23 100644
--- a/dist/hosts/pi/extension/runtime.js
+++ b/dist/hosts/pi/extension/runtime.js
@@ -1283,6 +1283,12 @@ function readNoOmissionFromEnv(env = process.env) {
   return NO_OMISSION_TRUTHY_VALUES.has(env[NO_OMISSION_ENV] ?? "");
 }
 
+// src/core/artifacts.ts
+import { mkdir, readFile, readdir, writeFile } from "node:fs/promises";
+import { homedir } from "node:os";
+import { join } from "node:path";
+import { randomUUID } from "node:crypto";
+
 // src/core/compaction-metadata.ts
 var NO_COMPACTION_METADATA = {
   authoritative: false,
@@ -1312,9 +1318,6 @@ function mergeCompactionMetadata(...values) {
   return buildCompactionMetadata(presentValues.every((value) => value.authoritative), ...kinds);
 }
 
-// src/core/reduce-utils.ts
-import { createHash } from "node:crypto";
-
 // src/core/text.ts
 var ANSI_CSI_PATTERN = new RegExp(String.raw`\u001B\[[0-?]*[ -/]*[@-~]`, "g");
 var ANSI_OSC_PATTERN = new RegExp(String.raw`\u001B\][^\u0007\u001B]*(?:\u0007|\u001B\\)`, "g");
@@ -1463,15 +1466,214 @@ function pluralize(count, noun) {
   return `${count} ${noun}s`;
 }
 
+// src/core/source.ts
+var DEFAULT_CLI_ARTIFACT_SOURCE = "cli";
+function normalizeArtifactSource(value) {
+  if (typeof value !== "string") {
+    return null;
+  }
+  const source = value.trim().toLowerCase().replace(/[_\s]+/gu, "-").replace(/[^a-z0-9.-]+/gu, "-").replace(/^-+|-+$/gu, "");
+  if (!source) {
+    return null;
+  }
+  if (source.startsWith("amazon-q") || source.startsWith("amazonq") || source.startsWith("aws-q")) {
+    return "amazon-q";
+  }
+  if (source.startsWith("claude-code") || source === "claude") {
+    return "claude-code";
+  }
+  if (source.startsWith("cline")) {
+    return "cline";
+  }
+  if (source.startsWith("codex")) {
+    return "codex";
+  }
+  if (source.startsWith("command-code") || source.startsWith("commandcode")) {
+    return "command-code";
+  }
+  if (source.startsWith("copilot-agent") || source.startsWith("github-copilot-agent")) {
+    return "copilot-agent";
+  }
+  if (source.startsWith("copilot-cli") || source.startsWith("github-copilot-cli")) {
+    return "copilot-cli";
+  }
+  if (source.startsWith("crush")) {
+    return "crush";
+  }
+  if (source.startsWith("cursor")) {
+    return "cursor";
+  }
+  if (source.startsWith("droid") || source.startsWith("factory-droid")) {
+    return "droid";
+  }
+  if (source.startsWith("gemini")) {
+    return "gemini-cli";
+  }
+  if (source.startsWith("grok-build") || source.startsWith("grokbuild") || source.startsWith("xai-grok-build")) {
+    return "grok-build";
+  }
+  if (source.startsWith("grok")) {
+    return "grok-cli";
+  }
+  if (source.startsWith("goose")) {
+    return "goose";
+  }
+  if (source.startsWith("antigravity") || source === "google-antigravity") {
+    return "antigravity";
+  }
+  if (source.startsWith("augment") || source.startsWith("auggie")) {
+    return "augment";
+  }
+  if (source.startsWith("openclaw")) {
+    return "openclaw";
+  }
+  if (source.startsWith("openhands")) {
+    return "openhands";
+  }
+  if (source.startsWith("openinterpreter") || source.startsWith("open-interpreter")) {
+    return "open-interpreter";
+  }
+  if (source.startsWith("openwebui") || source.startsWith("open-webui")) {
+    return "openwebui";
+  }
+  if (source.startsWith("opencode") || source.startsWith("open-code")) {
+    return "opencode";
+  }
+  if (source.startsWith("pi")) {
+    return "pi";
+  }
+  if (source.startsWith("plandex")) {
+    return "plandex";
+  }
+  if (source.startsWith("qoder")) {
+    return "qoder";
+  }
+  if (source.startsWith("qwen")) {
+    return "qwen-code";
+  }
+  if (source.startsWith("kimi")) {
+    return "kimi";
+  }
+  if (source.startsWith("mux")) {
+    return "mux";
+  }
+  if (source.startsWith("ruler")) {
+    return "ruler";
+  }
+  if (source.startsWith("vscode-copilot") || source.startsWith("vs-code-copilot")) {
+    return "vscode-copilot";
+  }
+  if (source === "direct" || source === "tokenjuice" || source === "wrap") {
+    return DEFAULT_CLI_ARTIFACT_SOURCE;
+  }
+  return source;
+}
+function resolveArtifactSource(input) {
+  return normalizeArtifactSource(input.metadata?.source) ?? DEFAULT_CLI_ARTIFACT_SOURCE;
+}
+
+// src/core/artifacts.ts
+var ARTIFACT_ID_PATTERN = /^tj_[0-9a-f-]{12}$/iu;
+var ARTIFACT_DIR_ENV = "TOKENJUICE_ARTIFACT_DIR";
+function extractCaptureTruncatedFlag(input) {
+  const value = input.metadata?.tokenjuiceCaptureTruncated;
+  return typeof value === "boolean" ? value : void 0;
+}
+function getDefaultArtifactDir() {
+  const artifactDir = process.env[ARTIFACT_DIR_ENV];
+  if (typeof artifactDir === "string" && artifactDir.trim()) {
+    return artifactDir.trim();
+  }
+  return join(homedir(), ".tokenjuice", "artifacts");
+}
+function resolveArtifactBaseDir(storeDir) {
+  return storeDir ?? getDefaultArtifactDir();
+}
+function isValidArtifactId(id) {
+  return ARTIFACT_ID_PATTERN.test(id);
+}
+function buildArtifactPaths(id, storeDir) {
+  if (!isValidArtifactId(id)) {
+    throw new Error(`invalid artifact id: ${id}`);
+  }
+  const base = resolveArtifactBaseDir(storeDir);
+  return {
+    id,
+    storage: "file",
+    path: join(base, `${id}.txt`),
+    metadataPath: join(base, `${id}.json`)
+  };
+}
+function buildMetadataOnlyPath(id, storeDir) {
+  if (!isValidArtifactId(id)) {
+    throw new Error(`invalid artifact id: ${id}`);
+  }
+  return join(resolveArtifactBaseDir(storeDir), `${id}.meta.json`);
+}
+async function storeArtifact(input, storeDir) {
+  const id = `tj_${randomUUID().slice(0, 12)}`;
+  const ref = buildArtifactPaths(id, storeDir);
+  await mkdir(resolveArtifactBaseDir(storeDir), { recursive: true, mode: 448 });
+  const captureTruncated = extractCaptureTruncatedFlag(input.input);
+  const artifact = {
+    id,
+    rawText: input.rawText,
+    metadata: {
+      createdAt: (/* @__PURE__ */ new Date()).toISOString(),
+      source: resolveArtifactSource(input.input),
+      classification: input.classification,
+      rawChars: input.stats?.rawChars ?? countTextChars(stripAnsi(input.rawText)),
+      ...input.input.toolName ? { toolName: input.input.toolName } : {},
+      ...input.input.command ? { command: input.input.command } : {},
+      ...typeof input.input.exitCode === "number" ? { exitCode: input.input.exitCode } : {},
+      ...captureTruncated !== void 0 ? { captureTruncated } : {},
+      ...input.stats ? { reducedChars: input.stats.reducedChars, ratio: input.stats.ratio } : {}
+    }
+  };
+  await Promise.all([
+    writeFile(ref.path, input.rawText, { encoding: "utf8", mode: 384 }),
+    writeFile(ref.metadataPath, JSON.stringify(artifact.metadata, null, 2), { encoding: "utf8", mode: 384 })
+  ]);
+  return ref;
+}
+async function storeArtifactMetadata(input, storeDir) {
+  const id = `tj_${randomUUID().slice(0, 12)}`;
+  const metadataPath = buildMetadataOnlyPath(id, storeDir);
+  const captureTruncated = extractCaptureTruncatedFlag(input.input);
+  const metadata = {
+    createdAt: (/* @__PURE__ */ new Date()).toISOString(),
+    source: resolveArtifactSource(input.input),
+    classification: input.classification,
+    rawChars: input.stats?.rawChars ?? countTextChars(stripAnsi(input.rawText)),
+    ...input.input.toolName ? { toolName: input.input.toolName } : {},
+    ...input.input.command ? { command: input.input.command } : {},
+    ...typeof input.input.exitCode === "number" ? { exitCode: input.input.exitCode } : {},
+    ...captureTruncated !== void 0 ? { captureTruncated } : {},
+    ...input.stats ? { reducedChars: input.stats.reducedChars, ratio: input.stats.ratio } : {}
+  };
+  await mkdir(resolveArtifactBaseDir(storeDir), { recursive: true, mode: 448 });
+  await writeFile(metadataPath, JSON.stringify(metadata, null, 2), { encoding: "utf8", mode: 384 });
+  return {
+    id,
+    storage: "file",
+    metadataPath,
+    metadata
+  };
+}
+
 // src/core/reduce-utils.ts
+import { createHash } from "node:crypto";
 function compactWhitespace(text) {
   return text.replace(/\s+/gu, " ").trim();
 }
 function shortHash(text) {
   return createHash("sha256").update(text).digest("hex").slice(0, 12);
 }
+var MIN_HASHED_CLIP_BUDGET_CHARS = 64;
 function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
-  if (countTextChars(text) <= maxChars) {
+  const boundedMaxChars = Math.max(0, Math.floor(maxChars));
+  const inputChars = countTextChars(text);
+  if (inputChars <= boundedMaxChars) {
     return { text };
   }
   if (noOmit) {
@@ -1480,16 +1682,27 @@ function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
       compaction: createPassthroughCompactionMetadata("no-omit-char-clip-passthrough")
     };
   }
-  const omitted = countTextChars(text) - maxChars;
-  const marker = `...[${omitted} chars omitted, sha256:${shortHash(text)}]...`;
-  const markerChars = countTextChars(marker);
-  if (maxChars <= markerChars) {
+  const buildMarker = (omitted2) => `...[${omitted2} chars omitted, sha256:${shortHash(text)}]...`;
+  let omitted = inputChars;
+  let marker = buildMarker(omitted);
+  let markerChars = countTextChars(marker);
+  if (boundedMaxChars <= markerChars) {
     return {
-      text: sliceTextChars(marker, 0, maxChars),
+      text: sliceTextChars(marker, 0, boundedMaxChars),
       compaction: createCompactionMetadata("hashed-middle-clip")
     };
   }
-  const bodyChars = maxChars - markerChars;
+  let bodyChars = boundedMaxChars - markerChars;
+  for (; ; ) {
+    const nextOmitted = inputChars - bodyChars;
+    if (nextOmitted === omitted) {
+      break;
+    }
+    omitted = nextOmitted;
+    marker = buildMarker(omitted);
+    markerChars = countTextChars(marker);
+    bodyChars = Math.max(0, boundedMaxChars - markerChars);
+  }
   const headChars = Math.ceil(bodyChars * 0.7);
   const tailChars = Math.max(0, bodyChars - headChars);
   return {
@@ -1497,6 +1710,12 @@ function clipMiddleWithHashStrict(text, maxChars, noOmit = false) {
     compaction: createCompactionMetadata("hashed-middle-clip")
   };
 }
+function clipMiddleWithHashToBudget(text, maxChars) {
+  if (!Number.isInteger(maxChars) || maxChars < MIN_HASHED_CLIP_BUDGET_CHARS) {
+    return { text };
+  }
+  return clipMiddleWithHashStrict(text, maxChars);
+}
 function minifyJsonLexically(rawText) {
   const text = rawText.trim();
   let output = "";
@@ -1687,9 +1906,9 @@ function buildInspectionSummary(input, rawText, noOmit = false) {
 }
 
 // src/core/rules.ts
-import { readdir, readFile, realpath } from "node:fs/promises";
-import { homedir } from "node:os";
-import { isAbsolute, join, relative, resolve } from "node:path";
+import { readdir as readdir2, readFile as readFile2, realpath } from "node:fs/promises";
+import { homedir as homedir2 } from "node:os";
+import { isAbsolute, join as join2, relative, resolve } from "node:path";
 import { fileURLToPath } from "node:url";
 
 // src/rules/archive/tar.json
@@ -7254,10 +7473,10 @@ async function listRuleFiles(root) {
   }
   const rootRealPath = resolvedRoot;
   async function walk(currentDir) {
-    const entries = (await readdir(currentDir, { withFileTypes: true })).sort((left, right) => left.name.localeCompare(right.name));
+    const entries = (await readdir2(currentDir, { withFileTypes: true })).sort((left, right) => left.name.localeCompare(right.name));
     const files = await Promise.all(
       entries.map(async (entry) => {
-        const fullPath = join(currentDir, entry.name);
+        const fullPath = join2(currentDir, entry.name);
         if (entry.isSymbolicLink()) {
           return [];
         }
@@ -7287,10 +7506,10 @@ async function listRuleFiles(root) {
   }
 }
 function userRulesRoot(customDir) {
-  return customDir ?? join(homedir(), ".config", "tokenjuice", "rules");
+  return customDir ?? join2(homedir2(), ".config", "tokenjuice", "rules");
 }
 function projectRulesRoot(cwd, customDir) {
-  return customDir ?? join(cwd ?? process.cwd(), ".tokenjuice", "rules");
+  return customDir ?? join2(cwd ?? process.cwd(), ".tokenjuice", "rules");
 }
 function sortRules(rules) {
   return [...rules].sort((left, right) => {
@@ -7307,7 +7526,7 @@ async function loadRuleDescriptorsFromRoot(root, source) {
   const files = await listRuleFiles(root);
   return await Promise.all(
     files.map(async (fullPath) => {
-      const parsed = JSON.parse(await readFile(fullPath, "utf8"));
+      const parsed = JSON.parse(await readFile2(fullPath, "utf8"));
       assertValidRule(parsed);
       return {
         source,
@@ -7590,207 +7809,6 @@ function normalizeExecutionInput(input) {
   };
 }
 
-// src/core/artifacts.ts
-import { mkdir, readFile as readFile2, readdir as readdir2, writeFile } from "node:fs/promises";
-import { homedir as homedir2 } from "node:os";
-import { join as join2 } from "node:path";
-import { randomUUID } from "node:crypto";
-
-// src/core/source.ts
-var DEFAULT_CLI_ARTIFACT_SOURCE = "cli";
-function normalizeArtifactSource(value) {
-  if (typeof value !== "string") {
-    return null;
-  }
-  const source = value.trim().toLowerCase().replace(/[_\s]+/gu, "-").replace(/[^a-z0-9.-]+/gu, "-").replace(/^-+|-+$/gu, "");
-  if (!source) {
-    return null;
-  }
-  if (source.startsWith("amazon-q") || source.startsWith("amazonq") || source.startsWith("aws-q")) {
-    return "amazon-q";
-  }
-  if (source.startsWith("claude-code") || source === "claude") {
-    return "claude-code";
-  }
-  if (source.startsWith("cline")) {
-    return "cline";
-  }
-  if (source.startsWith("codex")) {
-    return "codex";
-  }
-  if (source.startsWith("command-code") || source.startsWith("commandcode")) {
-    return "command-code";
-  }
-  if (source.startsWith("copilot-agent") || source.startsWith("github-copilot-agent")) {
-    return "copilot-agent";
-  }
-  if (source.startsWith("copilot-cli") || source.startsWith("github-copilot-cli")) {
-    return "copilot-cli";
-  }
-  if (source.startsWith("crush")) {
-    return "crush";
-  }
-  if (source.startsWith("cursor")) {
-    return "cursor";
-  }
-  if (source.startsWith("droid") || source.startsWith("factory-droid")) {
-    return "droid";
-  }
-  if (source.startsWith("gemini")) {
-    return "gemini-cli";
-  }
-  if (source.startsWith("grok-build") || source.startsWith("grokbuild") || source.startsWith("xai-grok-build")) {
-    return "grok-build";
-  }
-  if (source.startsWith("grok")) {
-    return "grok-cli";
-  }
-  if (source.startsWith("goose")) {
-    return "goose";
-  }
-  if (source.startsWith("antigravity") || source === "google-antigravity") {
-    return "antigravity";
-  }
-  if (source.startsWith("augment") || source.startsWith("auggie")) {
-    return "augment";
-  }
-  if (source.startsWith("openclaw")) {
-    return "openclaw";
-  }
-  if (source.startsWith("openhands")) {
-    return "openhands";
-  }
-  if (source.startsWith("openinterpreter") || source.startsWith("open-interpreter")) {
-    return "open-interpreter";
-  }
-  if (source.startsWith("openwebui") || source.startsWith("open-webui")) {
-    return "openwebui";
-  }
-  if (source.startsWith("opencode") || source.startsWith("open-code")) {
-    return "opencode";
-  }
-  if (source.startsWith("pi")) {
-    return "pi";
-  }
-  if (source.startsWith("plandex")) {
-    return "plandex";
-  }
-  if (source.startsWith("qoder")) {
-    return "qoder";
-  }
-  if (source.startsWith("qwen")) {
-    return "qwen-code";
-  }
-  if (source.startsWith("kimi")) {
-    return "kimi";
-  }
-  if (source.startsWith("mux")) {
-    return "mux";
-  }
-  if (source.startsWith("ruler")) {
-    return "ruler";
-  }
-  if (source.startsWith("vscode-copilot") || source.startsWith("vs-code-copilot")) {
-    return "vscode-copilot";
-  }
-  if (source === "direct" || source === "tokenjuice" || source === "wrap") {
-    return DEFAULT_CLI_ARTIFACT_SOURCE;
-  }
-  return source;
-}
-function resolveArtifactSource(input) {
-  return normalizeArtifactSource(input.metadata?.source) ?? DEFAULT_CLI_ARTIFACT_SOURCE;
-}
-
-// src/core/artifacts.ts
-var ARTIFACT_ID_PATTERN = /^tj_[0-9a-f-]{12}$/iu;
-var ARTIFACT_DIR_ENV = "TOKENJUICE_ARTIFACT_DIR";
-function extractCaptureTruncatedFlag(input) {
-  const value = input.metadata?.tokenjuiceCaptureTruncated;
-  return typeof value === "boolean" ? value : void 0;
-}
-function getDefaultArtifactDir() {
-  const artifactDir = process.env[ARTIFACT_DIR_ENV];
-  if (typeof artifactDir === "string" && artifactDir.trim()) {
-    return artifactDir.trim();
-  }
-  return join2(homedir2(), ".tokenjuice", "artifacts");
-}
-function resolveArtifactBaseDir(storeDir) {
-  return storeDir ?? getDefaultArtifactDir();
-}
-function isValidArtifactId(id) {
-  return ARTIFACT_ID_PATTERN.test(id);
-}
-function buildArtifactPaths(id, storeDir) {
-  if (!isValidArtifactId(id)) {
-    throw new Error(`invalid artifact id: ${id}`);
-  }
-  const base = resolveArtifactBaseDir(storeDir);
-  return {
-    id,
-    storage: "file",
-    path: join2(base, `${id}.txt`),
-    metadataPath: join2(base, `${id}.json`)
-  };
-}
-function buildMetadataOnlyPath(id, storeDir) {
-  if (!isValidArtifactId(id)) {
-    throw new Error(`invalid artifact id: ${id}`);
-  }
-  return join2(resolveArtifactBaseDir(storeDir), `${id}.meta.json`);
-}
-async function storeArtifact(input, storeDir) {
-  const id = `tj_${randomUUID().slice(0, 12)}`;
-  const ref = buildArtifactPaths(id, storeDir);
-  await mkdir(resolveArtifactBaseDir(storeDir), { recursive: true, mode: 448 });
-  const captureTruncated = extractCaptureTruncatedFlag(input.input);
-  const artifact = {
-    id,
-    rawText: input.rawText,
-    metadata: {
-      createdAt: (/* @__PURE__ */ new Date()).toISOString(),
-      source: resolveArtifactSource(input.input),
-      classification: input.classification,
-      rawChars: input.stats?.rawChars ?? countTextChars(stripAnsi(input.rawText)),
-      ...input.input.toolName ? { toolName: input.input.toolName } : {},
-      ...input.input.command ? { command: input.input.command } : {},
-      ...typeof input.input.exitCode === "number" ? { exitCode: input.input.exitCode } : {},
-      ...captureTruncated !== void 0 ? { captureTruncated } : {},
-      ...input.stats ? { reducedChars: input.stats.reducedChars, ratio: input.stats.ratio } : {}
-    }
-  };
-  await Promise.all([
-    writeFile(ref.path, input.rawText, { encoding: "utf8", mode: 384 }),
-    writeFile(ref.metadataPath, JSON.stringify(artifact.metadata, null, 2), { encoding: "utf8", mode: 384 })
-  ]);
-  return ref;
-}
-async function storeArtifactMetadata(input, storeDir) {
-  const id = `tj_${randomUUID().slice(0, 12)}`;
-  const metadataPath = buildMetadataOnlyPath(id, storeDir);
-  const captureTruncated = extractCaptureTruncatedFlag(input.input);
-  const metadata = {
-    createdAt: (/* @__PURE__ */ new Date()).toISOString(),
-    source: resolveArtifactSource(input.input),
-    classification: input.classification,
-    rawChars: input.stats?.rawChars ?? countTextChars(stripAnsi(input.rawText)),
-    ...input.input.toolName ? { toolName: input.input.toolName } : {},
-    ...input.input.command ? { command: input.input.command } : {},
-    ...typeof input.input.exitCode === "number" ? { exitCode: input.input.exitCode } : {},
-    ...captureTruncated !== void 0 ? { captureTruncated } : {},
-    ...input.stats ? { reducedChars: input.stats.reducedChars, ratio: input.stats.ratio } : {}
-  };
-  await mkdir(resolveArtifactBaseDir(storeDir), { recursive: true, mode: 448 });
-  await writeFile(metadataPath, JSON.stringify(metadata, null, 2), { encoding: "utf8", mode: 384 });
-  return {
-    id,
-    storage: "file",
-    metadataPath,
-    metadata
-  };
-}
-
 // src/core/github-actions-summary.ts
 var GITHUB_ACTIONS_EXIT_PATTERN = /^Error: Process completed with exit code (\d+)\.?$/u;
 var GITHUB_ACTIONS_RUN_PATTERN = /^(?:##\[group\]|::group::)?Run\s+(.+)$/u;
@@ -8991,6 +9009,59 @@ function getCompactionSkipReason(command, rawText, result, options) {
 }
 
 // src/core/integrations/compact-bash-result.ts
+function buildBoundedFallbackResult(rawText, result, clipped) {
+  const rawChars = result?.stats.rawChars ?? countTextChars(rawText);
+  const reducedChars = countTextChars(clipped.text);
+  return {
+    ...result ?? {
+      classification: {
+        family: "preserved-output",
+        confidence: 0
+      }
+    },
+    inlineText: clipped.text,
+    ...clipped.compaction ? { compaction: mergeCompactionMetadata(result?.compaction, clipped.compaction) } : {},
+    stats: {
+      rawChars,
+      reducedChars,
+      ratio: rawChars === 0 ? 1 : reducedChars / rawChars
+    }
+  };
+}
+async function applyBoundedKeepFallback(input, keep, executionInput) {
+  const maxInlineChars = input.boundedKeep?.maxInlineChars;
+  if (typeof maxInlineChars !== "number" || !Number.isInteger(maxInlineChars) || maxInlineChars < MIN_HASHED_CLIP_BUDGET_CHARS) {
+    return keep;
+  }
+  const clipped = clipMiddleWithHashToBudget(keep.rawText, maxInlineChars);
+  if (clipped.text === keep.rawText) {
+    return keep;
+  }
+  let result = buildBoundedFallbackResult(keep.rawText, keep.result, clipped);
+  if (!keep.result) {
+    const artifactInput = {
+      input: executionInput,
+      rawText: keep.rawText,
+      classification: result.classification,
+      stats: result.stats
+    };
+    if (input.storeRaw) {
+      result = {
+        ...result,
+        rawRef: await storeArtifact(artifactInput)
+      };
+    } else {
+      await storeArtifactMetadata(artifactInput);
+    }
+  }
+  return {
+    action: "rewrite",
+    rawText: keep.rawText,
+    usedTrustedFullText: keep.usedTrustedFullText,
+    result,
+    fallbackReason: keep.reason
+  };
+}
 function resolveInspectionPolicy(input) {
   if (input.inspectionPolicy) {
     return input.inspectionPolicy;
@@ -9038,12 +9109,12 @@ async function compactBashResult(input) {
   };
   const inspectionSkipReason = getOutputAwareInspectionSkipReason(resolveInspectionPolicy(input), executionInput);
   if (inspectionSkipReason) {
-    return {
+    return applyBoundedKeepFallback(input, {
       action: "keep",
       reason: inspectionSkipReason,
       rawText,
       usedTrustedFullText
-    };
+    }, executionInput);
   }
   const options = {
     ...typeof input.cwd === "string" && input.cwd.trim() ? { cwd: input.cwd } : {},
@@ -9055,13 +9126,13 @@ async function compactBashResult(input) {
   const result = await reduceExecution(executionInput, options);
   const skipReason = getCompactionSkipReason(command, rawText, result, input);
   if (skipReason) {
-    return {
+    return applyBoundedKeepFallback(input, {
       action: "keep",
       reason: skipReason,
       rawText,
       usedTrustedFullText,
       result
-    };
+    }, executionInput);
   }
   return {
     action: "rewrite",
@@ -9270,7 +9341,7 @@ function buildCompactionNotice(result, fullOutputPath) {
   const suffix = hints.length > 0 ? ` (${hints.join(", ")})` : "";
   return `tokenjuice compacted bash output${suffix}`;
 }
-function buildTokenjuiceDetails(result) {
+function buildTokenjuiceDetails(result, fallbackReason) {
   const rawChars = typeof result.stats?.rawChars === "number" ? result.stats.rawChars : 0;
   const reducedChars = typeof result.stats?.reducedChars === "number" ? result.stats.reducedChars : 0;
   return {
@@ -9278,7 +9349,8 @@ function buildTokenjuiceDetails(result) {
     rawChars,
     reducedChars,
     savedChars: Math.max(0, rawChars - reducedChars),
-    ...typeof result.classification?.matchedReducer === "string" ? { reducer: result.classification.matchedReducer } : {}
+    ...typeof result.classification?.matchedReducer === "string" ? { reducer: result.classification.matchedReducer } : {},
+    ...fallbackReason ? { fallbackReason } : {}
   };
 }
 function mergeDetails(existingDetails, tokenjuiceDetails) {
diff --git a/dist/hosts/shared/pre-tool-wrap.d.ts b/dist/hosts/shared/pre-tool-wrap.d.ts
index d846d5b6e815105d014c762ddeec159bf07acfe0..59d5f23e2b2d2956d77e355749f5ffb2d0282df9 100644
--- a/dist/hosts/shared/pre-tool-wrap.d.ts
+++ b/dist/hosts/shared/pre-tool-wrap.d.ts
@@ -74,6 +74,9 @@ export declare function buildWrappedCommand(params: {
     shellPath: string;
     command: string;
     source?: string;
+    store?: boolean;
+    maxInlineChars?: number;
+    recordStats?: boolean;
     nodePath?: string;
 }): string;
 /**
diff --git a/dist/hosts/shared/pre-tool-wrap.js b/dist/hosts/shared/pre-tool-wrap.js
index 1397fcccd6829e05d5687c9219a3e2685d15ce10..f04ce8995da064e5d0b2702554e0ef1d43c5c25b 100644
--- a/dist/hosts/shared/pre-tool-wrap.js
+++ b/dist/hosts/shared/pre-tool-wrap.js
@@ -151,7 +151,12 @@ export function buildWrappedCommand(params) {
         ? `${shellQuote(nodePath)} ${shellQuote(params.wrapLauncher)}`
         : shellQuote(params.wrapLauncher);
     const sourceArgs = params.source ? ` --source ${shellQuote(params.source)}` : "";
-    return `${launcherCommand} wrap${sourceArgs} -- ${shellQuote(params.shellPath)} -lc ${shellQuote(params.command)}`;
+    const storeArg = params.store ? " --store" : "";
+    const maxInlineArg = typeof params.maxInlineChars === "number"
+        ? ` --max-inline-chars ${params.maxInlineChars}`
+        : "";
+    const recordStatsArg = params.recordStats === false ? " --no-record-stats" : "";
+    return `${launcherCommand} wrap${sourceArgs}${storeArg}${maxInlineArg}${recordStatsArg} -- ${shellQuote(params.shellPath)} -lc ${shellQuote(params.command)}`;
 }
 /**
  * Detect whether a command has already been wrapped by tokenjuice. Matches
diff --git a/dist/hosts/shared/tool-result.d.ts b/dist/hosts/shared/tool-result.d.ts
index 7c9d6aabc54b98751cab2bb8f4f34ba66e115fbd..04d4b9bbc59009c142d74a3ecd2660dde007e526 100644
--- a/dist/hosts/shared/tool-result.d.ts
+++ b/dist/hosts/shared/tool-result.d.ts
@@ -1,15 +1,17 @@
 import type { CompactResult } from "../../types.js";
+import type { CompactBashResultKeepReason } from "../../core/integrations/compact-bash-result.js";
 export type TokenjuiceDetails = {
     compacted: true;
     rawChars: number;
     reducedChars: number;
     savedChars: number;
     reducer?: string;
+    fallbackReason?: CompactBashResultKeepReason;
 };
 export declare function extractTextContent(content: unknown): string;
 export declare function buildBypassNotice(fullOutputPath?: string): string;
 export declare function buildCompactionNotice(result: CompactResult, fullOutputPath?: string): string;
-export declare function buildTokenjuiceDetails(result: CompactResult): TokenjuiceDetails;
+export declare function buildTokenjuiceDetails(result: CompactResult, fallbackReason?: CompactBashResultKeepReason): TokenjuiceDetails;
 export declare function mergeDetails(existingDetails: unknown, tokenjuiceDetails: TokenjuiceDetails): {
     tokenjuice: TokenjuiceDetails;
 };
diff --git a/dist/hosts/shared/tool-result.js b/dist/hosts/shared/tool-result.js
index 8d244427e4e9e3246431d6c88e53ebd01417f0df..3bb08cfa9c022800073cdf9f1cb8f28e68b04cfd 100644
--- a/dist/hosts/shared/tool-result.js
+++ b/dist/hosts/shared/tool-result.js
@@ -26,7 +26,7 @@ export function buildCompactionNotice(result, fullOutputPath) {
     const suffix = hints.length > 0 ? ` (${hints.join(", ")})` : "";
     return `tokenjuice compacted bash output${suffix}`;
 }
-export function buildTokenjuiceDetails(result) {
+export function buildTokenjuiceDetails(result, fallbackReason) {
     const rawChars = typeof result.stats?.rawChars === "number" ? result.stats.rawChars : 0;
     const reducedChars = typeof result.stats?.reducedChars === "number" ? result.stats.reducedChars : 0;
     return {
@@ -37,6 +37,7 @@ export function buildTokenjuiceDetails(result) {
         ...(typeof result.classification?.matchedReducer === "string"
             ? { reducer: result.classification.matchedReducer }
             : {}),
+        ...(fallbackReason ? { fallbackReason } : {}),
     };
 }
 export function mergeDetails(existingDetails, tokenjuiceDetails) {
